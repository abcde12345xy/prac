barplot(airquality$Ozone, 
  main = "Ozone Concenteration in air", 
  xlab = "ozone levels", 
  col = "blue", 
  horiz = FALSE)
  
hist(airquality$Temp, 
    main ="Mumbai Maximum Temperature",
    xlab ="Temperature(Fahrenheit)",
    xlim = c(50, 125), 
    col ="yellow",
    freq = TRUE)

boxplot(airquality$Wind, 
  main = "Average wind speed",
  xlab = "Miles per hour", 
  ylab = "Wind",
  col = "orange", 
  border = "brown",
  horizontal = TRUE, 
  notch = TRUE)